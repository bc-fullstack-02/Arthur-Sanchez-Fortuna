// não veio automaticamnete então criei o arquivo
const {getDefaultConfig} = require('expo/metro-config');
module.exports = getDefaultConfig(__dirname);
// isso aki é uma lib que vai fazer umas configs para exibir o aplicativo,
// pelo q entendi isso aki serve pro auto reload/ refresh
// aula 1 min 1h e 23:30