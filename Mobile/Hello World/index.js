// não veio automaticamnete então criei o arquivo
import { registerRootComponent } from "expo";
import App from "./App";

// isso aki em resumo diz q o App é o componente raiz para o registerRootComponent
// explicação do registerRootComponent aula 1 min 1h e 21
registerRootComponent(App);

// index é o 1° arquivo que vai ser lido na aplicação